//Filtering Array
//Write JavaScript statements that will produce the output mentioned in output 
//format from array in input formamat

const inputWords = ["spray", "limit", "elite", "exuberant", "destruction", "present"];
const result = inputWords.slice(-3);
console.log(result);