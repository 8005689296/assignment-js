//Uppercase ArrayEasy
//Create a new array whose elements is in uppercase of words present in the original array.

const arr =  ["avengers", "captain america", "ironman", "black panther"];
const re = arr.map(arr  =>arr.toUpperCase());
console.log(re);